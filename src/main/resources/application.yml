server:
   port: 8888

spring:
   application:
      name: config-server

   cloud:
      config:
         server:
            git:
               uri: https://github.com/micro-services-mrbin2903/config-repo
               default-label: main
            bootstrap: false  
 